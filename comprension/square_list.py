# Se utiliza la comprensión de listas para iterar sobre los números del 0 al 10 (exclusivamente), 
# utilizando range(0, 11).
# Para cada número x en ese rango, se eleva x al cuadrado usando x**2.
mylist = [x**2 for x in range(0,11)]
print(mylist) #crear una lista
